services:
  app:
    build: .
    container_name: template_app
    env_file: .env
    environment:
      DB_HOST: host.docker.internal
      NODE_ENV: production
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${PORT:-8000}:${PORT:-3000}"
